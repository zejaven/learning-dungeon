import visual.VisualPageLoad;

public class Playground {
    public static void main(String[] args) {
        // The same document under the five conditions that actually differ in
        // production, priced in round trips rather than in milliseconds.
        VisualPageLoad.compareRoundTrips();

        System.out.println("Every real optimisation is a way of deleting a row from that table.");
    }
}
