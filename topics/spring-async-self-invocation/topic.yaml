id: spring-async-self-invocation
languages: [en, ru]
title:
  en: '@Async and Self-Invocation'
  ru: '@Async и self-invocation'
category:
  en: Spring / Async
  ru: Spring / Асинхронность
type: SPRING
mode: theory
aiProvider: codex
aiModel: gpt-5.5
categoryId: spring
difficulty: 2
catalogId: spring-12
style: Real world
summary:
  en: >
    What @EnableAsync turns on, how @Async methods are executed through a Spring
    proxy and an Executor, and why a call from another method of the same class
    usually bypasses that proxy and runs synchronously.
  ru: >
    Что включает @EnableAsync, как @Async-методы выполняются через Spring proxy и
    Executor, и почему вызов из другого метода того же класса обычно обходит этот
    proxy и выполняется синхронно.

assistantExample:
  en: 'e.g. Why does @Async not run in a new thread when one method of a service calls another method of the same service?'
  ru: 'например: Почему @Async не запускается в новом потоке, когда один метод сервиса вызывает другой метод того же сервиса?'

missionsFile: quiz.yaml
