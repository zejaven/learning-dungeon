# Вопросы с собеседования (eRaga Infosystems)

## Знакомство

1. Расскажите о себе вкратце: чем занимались и с каким стеком работали?

## Java: память и сборка мусора

2. Как Java работает с памятью и каким образом она её очищает (Garbage Collector)?
3. Из каких основных частей состоит память JVM?
4. Что представляет собой стек и что в нём хранится?
5. Как очищается стек?
6. Может ли стек переполниться? Как этого можно добиться?
7. Есть приложение, которое со временем начинает потреблять много памяти и может упасть. Каким образом определить причину проблемы (помимо просмотра кода)?
8. Что такое профилировщики и как они работают — в runtime или нет?
9. Если проблема возникает на проде, как её можно изучить? (heap dump / thread dump)
10. Какие типы ссылок существуют в Java?
11. Для чего были введены слабые ссылки (WeakReference)?
12. Реализуйте кэш, который заполняется вручную, но при нехватке памяти Java сама его очистит, чтобы приложение не упало.

## Java Collections

13. В чём разница между `ArrayList` и `LinkedList` по операциям (вставка, чтение)?
14. Вставка в `ArrayList`: всегда ли сложность O(N)? Какие ещё операции могут происходить при вставке (расширение массива, load factor)?
15. Есть ли ограничение на максимальное количество элементов в `ArrayList`? Чему оно равно?
16. Что займёт больше памяти при одинаковом большом количестве элементов — `ArrayList` или `LinkedList`? Почему?
17. Как `HashMap` хранит данные в памяти, что используется внутри (бакеты, связный список, красно-чёрное дерево) и какой контракт должны поддерживать объекты-ключи (`equals` / `hashCode`)?

## Многопоточность (Concurrency)

18. Какой у вас опыт работы с concurrency?
19. Что такое семафор?
20. Что произойдёт, если модифицировать `ArrayList` из двух потоков одновременно?
21. Как избежать `ConcurrentModificationException`? Как именно выглядит синхронизация (метод/блок, на каком объекте)?
22. Какие альтернативы ключевому слову `synchronized` существуют? (`ReentrantLock`, `ReentrantReadWriteLock`, `StampedLock` и т.д.)
23. Что ещё, кроме блокировок и `synchronized`, можно использовать для работы в многопоточной среде? (атомики, concurrent-коллекции)
24. Знаете ли concurrent-коллекции для списков? (`CopyOnWriteArrayList`)
25. Что знаете про `wait` / `notify` / `notifyAll` и как они используются внутри `synchronized`-блоков?
26. Освобождает ли поток монитор (блокировку), когда вызывает `wait`? Могут ли другие потоки в это время войти в `synchronized`-блок?
27. Как запустить параллельно несколько задач, ограничивая количество одновременно работающих? (Thread Pool)
28. Нужно из main-потока запустить 10 задач параллельно, и продолжить работу main только после того, как все 10 задач завершатся. Как это реализовать?
29. Если потоков всего 4, а задач 10 — как организовать выполнение и дождаться всех задач (не делая `shutdown` у `ExecutorService`)?

## Spring Framework

30. Что такое Spring и для чего он был создан?
31. В чём заключается Inversion of Control?
32. Какими способами можно объявить бины в Spring, чтобы они попали в контекст?
33. Что такое аннотация `@Configuration` и как она используется?
34. Где применяется аннотация `@Bean`? Всегда ли только в классах с `@Configuration`?
35. В чём отличие между `@Repository` и `@Service`? (обёртка над исключениями БД, JDBC-исключения)
36. Что произойдёт, если у бинов возникнет циклическая зависимость (A → B → C → D → A)? Как этого избежать? Что делать, если рефакторинг невозможен? (`@Lazy`)

## Spring Transactions

37. **Задача 1.** Контроллер вызывает `checkSomething` (без `@Transactional`), который вызывает `save` (с `@Transactional`), внутри `save` идёт `entityManager.persist`. Сохранится ли компания в БД? Почему?
38. Какие есть варианты решить эту задачу, не меняя код контроллера и не добавляя `@Transactional` напрямую на `checkSomething`? (программное управление транзакциями через `TransactionTemplate`, self-инъекция бина через `@Lazy`)
39. **Задача 2.** Метод помечен `@Transactional`, внутри `persist` сущности и затем HTTP-вызов внешнего сервиса, который кидает исключение. Сохранится ли запись в БД? Всегда ли транзакция откатится?
40. Какие состояния сущности в JPA вы знаете? (`detached` и т.д.)
41. На каких типах исключений `@Transactional` по умолчанию откатывает транзакцию? Откатится ли транзакция на `IOException`? (`RuntimeException` vs checked, `rollbackFor`)
42. Работали ли вы с событиями Spring? Как выполнить действие после успешного коммита (или отката) транзакции? (`@TransactionalEventListener`)

## JPA / Hibernate / БД

43. Есть сущности `Person` и `Cat`: у одного человека может быть несколько кошек, у одной кошки — только один человек. Как это отобразить в схеме БД и в виде JPA-сущностей? (`@OneToMany` / `@ManyToOne`)
44. Как реализовать ту же связь, если мы не хотим хранить `person_id` в таблице `cat`? (промежуточная join-таблица)
45. Как при использовании промежуточной таблицы гарантировать, что одна кошка привязана только к одному человеку? (`UNIQUE`-constraint на `cat_id`)
46. Напишите один SQL-запрос: достать всех людей вместе с их кошками, при этом людей без кошек тоже выводить. (LEFT JOIN)
47. Напишите один SQL-запрос: достать только тех людей, у которых есть кошки, вместе с кошками. (INNER JOIN / RIGHT JOIN)
48. Чем отличаются `LEFT JOIN`, `INNER JOIN` и `RIGHT JOIN`?
49. Знакомы ли вы с проблемой N+1? Сталкивались ли сами?
50. Знакомы ли с JPA-блокировками (пессимистическая и оптимистическая)? Использовали ли их?
51. Чем отличаются пессимистическая и оптимистическая блокировки и для чего они нужны?
52. Какой SQL-запрос будет сгенерирован при `PESSIMISTIC_WRITE`-блокировке? (`SELECT ... FOR UPDATE`)
53. Какой SQL-запрос будет сгенерирован при `PESSIMISTIC_READ`-блокировке? (`SELECT ... FOR SHARE`)

## Сетевые технологии / Клиент-сервер

54. Есть клиент-серверное приложение (браузер + сервер). На сервере произошло событие — как уведомить о нём клиента? Какие технологии можно использовать?
55. Что такое long polling? Чем он отличается от обычных HTTP-запросов и как реализуется?
56. Какая ещё технология подходит для двунаправленного общения сервера с клиентом? (WebSocket)
57. По какому транспортному протоколу работают WebSocket — TCP или UDP?
