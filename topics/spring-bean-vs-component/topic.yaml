id: spring-bean-vs-component
title:
  en: '@Bean vs @Component in Spring'
  ru: '@Bean и @Component в Spring'
category:
  en: Spring / Bean Registration
  ru: Spring / Регистрация бинов
type: SPRING
mode: theory
aiProvider: codex
aiModel: gpt-5.5
categoryId: spring
difficulty: 2
catalogId: spring-1
style: Real world
summary:
  en: >
    How @Component and @Bean both create Spring beans, why @Component is discovered
    by classpath scanning while @Bean is declared explicitly in configuration, and
    when each choice is cleaner in production code.
  ru: >
    Как @Component и @Bean оба создают Spring-бины, почему @Component находится
    через сканирование classpath, а @Bean объявляется явно в конфигурации, и когда
    каждый вариант чище в production-коде.

assistantExample:
  en: 'e.g. Why would I use @Bean for a DataSource but @Component for OrderService?'
  ru: 'например: Почему для DataSource я бы использовал @Bean, а для OrderService — @Component?'

missionsFile: quiz.yaml
