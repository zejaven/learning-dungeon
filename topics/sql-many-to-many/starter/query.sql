-- The schema (left panel) has employees, courses, and a many-to-many
-- enrollments table linking them.
--
-- Mission 1: show each course name and how many employees are enrolled.
-- Mission 2: keep only courses with MORE THAN 10 enrolled employees (id, name).
--
-- Edit this query and press "Run query".
SELECT * FROM courses;
