// The client. It should depend only on PaymentProcessor, the target interface.
public class CheckoutService {

    public void checkout(int cents) {
        // TODO: delegate to a PaymentProcessor field.
    }
}
