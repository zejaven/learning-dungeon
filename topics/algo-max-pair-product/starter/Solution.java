public class Solution {

    /**
     * Returns the largest product of any TWO numbers in nums (length >= 2).
     *
     * Watch out: two large NEGATIVE numbers multiply to a large positive, so the
     * answer isn't always the two largest values.
     *
     * Examples:
     *   {1, 2, 3, 4}     -> 12   (3 * 4)
     *   {-10, -3, 1, 2}  -> 30   (-10 * -3)
     */
    public int maxPairProduct(int[] nums) {
        // TODO: implement me, then press "Run tests".
        return 0;
    }
}
