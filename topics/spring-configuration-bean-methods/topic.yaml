id: spring-configuration-bean-methods
title:
  en: '@Configuration and @Bean Methods'
  ru: '@Configuration и @Bean-методы'
category:
  en: Spring / Core Container
  ru: Spring / Core Container
type: SPRING
mode: theory
aiProvider: codex
aiModel: gpt-5.5
categoryId: spring
difficulty: 2
catalogId: spring-6
style: Real world
summary:
  en: >
    What @Configuration means in Spring, how its @Bean methods become bean
    definitions, why full configuration classes proxy method calls, and when
    @Bean methods can safely live in non-@Configuration classes.
  ru: >
    Что означает @Configuration в Spring, как его @Bean-методы становятся
    bean definitions, зачем полноценные конфигурационные классы проксируют
    вызовы методов и когда @Bean-методы могут безопасно находиться вне классов
    с @Configuration.

assistantExample:
  en: 'e.g. Can I put a @Bean method inside a @Component, and what changes compared with @Configuration?'
  ru: 'например: Можно ли поместить @Bean-метод внутрь @Component и что изменится по сравнению с @Configuration?'

missionsFile: quiz.yaml
