id: spring-repository-vs-service
title:
  en: '@Repository vs @Service in Spring'
  ru: '@Repository и @Service в Spring'
category:
  en: Spring / Stereotypes and Data Access
  ru: Spring / Стереотипы и доступ к данным
type: SPRING
mode: theory
aiProvider: codex
aiModel: gpt-5.5
categoryId: spring
difficulty: 2
catalogId: spring-7
style: Real world
summary:
  en: >
    How @Repository and @Service both act as Spring stereotype annotations,
    what layer each one communicates, and why @Repository can participate in
    persistence exception translation while @Service normally adds no special
    runtime behavior by itself.
  ru: >
    Как @Repository и @Service работают как stereotype annotations в Spring,
    какой слой приложения они обозначают и почему @Repository может участвовать
    в трансляции persistence exceptions, а @Service обычно сам по себе не
    добавляет особого runtime-поведения.

assistantExample:
  en: 'e.g. Why does Spring have both @Repository and @Service if both are components?'
  ru: 'например: Зачем в Spring есть и @Repository, и @Service, если оба являются components?'

missionsFile: quiz.yaml
