1. I have service just for writing data to the database. It has one API function just for this. Many other services call this service using API to write the data. The data is quite big, for example, 30 MB. We're using PostgreSQL database. What can be done in order to improve writing performance of these services?
2. What is Thread Pool in Java and for what reasons it can be used?
3. What is JWT? Should we use it for client-server communication or server-to-server communication? When you should use session token and when JWT token?
4. What is cross-site scripting (XSS)?
5. What is CSRF?
6. Let's imagine that some website works very slow. Having this information, what will you do to find the root cause?